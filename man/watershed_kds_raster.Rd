% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kdi_raster.R
\name{watershed_kds_raster}
\alias{watershed_kds_raster}
\title{calculate a raster of kernel diameter slope for AMS3D}
\usage{
watershed_kds_raster(
  las,
  bandwidth_intercept = 0,
  limits = c(0, 1),
  ground_height = NULL
)
}
\arguments{
\item{las}{a point cloud as lidR LAS object}

\item{bandwidth_intercept}{a fixed value for kernel_diameter_intercept, which
reduces the crown diameters by the given value before calculating the ratio
of crown diameter to tree height}

\item{limits}{a numeric vector with minimum and maximum allowed values for
the ratio}

\item{ground_height}{(optional) either
*NULL, indicating that the point cloud is normalized, or
*a \link[terra:SpatRaster-class]{SpatRaster} digital terrain model, or
*a list of arguments to the
\link[lidR:rasterize]{lidR rasterize_terrain()} function to normalize
the point cloud.}
}
\description{
The function calculates a raster with values for the kernel diameter slope
as input for the AMS3D algorithm. It segments the tree crowns with
the watershed algorithm, calculates a ratio of crown diameter to tree height
for each tree, and converts this into a raster
}
